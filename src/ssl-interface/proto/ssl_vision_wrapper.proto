syntax = "proto2";
import "ssl_vision_detection.proto";
import "ssl_vision_geometry.proto";

/* Definition of Protobuf message from SSL Vision */
message SSLWrapperPacket
{
  optional SSLDetectionFrame detection = 1;
  optional SSLGeometryData geometry = 2;
}
